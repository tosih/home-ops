---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: romm-oidc
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: 1password-sdk
  target:
    name: romm-oidc-secret
    creationPolicy: Owner
  data:
    - secretKey: OIDC_CLIENT_ID
      remoteRef:
        key: romm/oidc_client_id
    - secretKey: OIDC_CLIENT_SECRET
      remoteRef:
        key: romm/oidc_client_secret
