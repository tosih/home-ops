---
apiVersion: v1
kind: ConfigMap
metadata:
  name: filebrowser-config
  namespace: cloud
data:
  config.yaml: |
    server:
      port: 80
      baseURL: "/"
      cacheDir: /home/filebrowser/data/tmp
      sources:
        - path: /data
          config:
            defaultEnabled: true
    
    auth:
      methods:
        password:
          enabled: false
        oidc:
          enabled: true
          issuerUrl: "https://pid.tosih.org"
          scopes: "email openid profile"
          userIdentifier: "preferred_username"
          createUser: true
    
    userDefaults:
      darkMode: true
      permissions:
        admin: false
        modify: true
        share: true
        download: true
        create: true
        delete: true
