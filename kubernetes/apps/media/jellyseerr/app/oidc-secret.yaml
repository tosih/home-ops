---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: jellyseerr-oidc
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: 1password-sdk
  target:
    name: jellyseerr-oidc-secret
    creationPolicy: Owner
  data:
    - secretKey: OIDC_CLIENT_ID
      remoteRef:
        key: jellyseerr/oidc_client_id
    - secretKey: OIDC_CLIENT_SECRET
      remoteRef:
        key: jellyseerr/oidc_client_secret
